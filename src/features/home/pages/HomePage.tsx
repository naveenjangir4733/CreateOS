export default function HomePage() {
  return (
    <section className="p-4">
      <h1 className="text-xl font-semibold">Home</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Routing is now driven by `RouterProvider` and nested routes.
      </p>
    </section>
  );
}
